:80 {
    # Route API requests to the backend
    handle_path /api/* {
        reverse_proxy backend:8000
    }

    # Route all other requests to the frontend dev server
    handle {
        reverse_proxy frontend:5173
    }
}
